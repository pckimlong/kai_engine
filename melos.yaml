name: kai_engine_workspace
repository: https://github.com/pckimlong/kai_engine

packages:
  - packages/*

command:
  version:
    branch: main
    linkToCommits: true
    workspaceChangelog: true
    releaseUrl: true

scripts:
  analyze:
    description: Analyze all packages
    run: melos exec --fail-fast -- "if [ \"$MELOS_PACKAGE_NAME\" = \"prompt_block\" ]; then dart analyze; else flutter analyze --no-fatal-infos; fi"

  format:
    description: Format all packages
    run: melos exec -- "dart format --set-exit-if-changed ."

  test:
    description: Run tests for all packages
    run: melos exec --fail-fast -- "if [ \"$MELOS_PACKAGE_NAME\" = \"prompt_block\" ]; then dart test; else flutter test; fi"
    packageFilters:
      dirExists: test

  test:coverage:
    description: Run tests with coverage
    run: melos exec --fail-fast -- "if [ \"$MELOS_PACKAGE_NAME\" = \"prompt_block\" ]; then dart test; else flutter test --coverage; fi"
    packageFilters:
      dirExists: test

  build:runner:
    description: Run build_runner for all packages
    run: melos exec --fail-fast -- "dart run build_runner build --delete-conflicting-outputs"
    packageFilters:
      dependsOn: build_runner
